---
title: RATL Specification
---

RATL (**R** **A**rray Manipula**T**ion **L**anguage) is a stack-based, esoteric programming language inspired by [MATL](https://github.com/lmendo/MATL). It leverages R's powerful statistical and matrix capabilities through a concise, postfix syntax, making it highly suitable for code golf.

**Total symbols: 427** (all 2-byte or shorter)

## Getting Started

RATL programs are sequences of tokens evaluated left to right on a stack. No variable declarations, no semicolons — just push values and apply operations.

### Basic Arithmetic
```
3 5 +       → 8        # push 3, push 5, add
10 3 /      → 3.333...  # divide
2 3 ^       → 8        # power
```

### Arrays
```
[1 2 3] l    → 3        # length
[1 2 3] s    → 6        # sum
[1 2 3] m    → 2        # mean
[3 1 2] O    → 1 2 3    # sort
```

### Blocks (Higher-Order Functions)
```
[1 2 3] {2 *} q    → 2 4 6      # map: double each
[1 2 3 4] {2 %} e  → 2 4         # filter: keep evens
[1 2 3 4] {+} y    → 10          # reduce: sum
```

### Control Flow
```
3 5 > ? 'yes' ; 'no' ]    → 'no'    # conditional
1 5 ( p )                  → 1 2 3 4 5  # for-each
```

***

## Language Overview

### Design Philosophy

RATL is designed as a **stack-based array manipulation language** for code golf. Its core model is:

1. **Stack** — all operations work on a single global stack
2. **Arrays** — the primary data structure for bulk operations
3. **Blocks** — first-class code fragments for higher-order operations

### Symbol Tiers

RATL organizes its symbol set into three conceptual tiers:

**Tier 1 — Core** (stack manipulation, arrays, blocks, control flow, arithmetic)
: 105 symbols across 5 categories

**Tier 2 — Toolkit** (grouping, mapping, reduction, matrix operations, strings, sets)
: 68 symbols across 4 categories

**Tier 3 — Extensions** (statistics, distributions, filesystem, shell access, meta-programming)
: 220 symbols across 7 categories

**Note:** This document excludes unsafe operations by default. Pass `--allow-unsafe-operations` to include them.

***

## The Stack

RATL uses a single global stack (an R `list`). Every operation interacts with this stack — pushing, popping, or transforming elements.

### Stack Notation

Throughout this specification, stack effects are described using the notation:

- `$1` — top of stack (most recently pushed)
- `$2` — second element from top
- `$n` — nth element from top
- `n → m` — operation consumes n elements, produces m elements

For example, `+` has stack effect `2 → 1`: it pops two values and pushes their sum.

### Stack Manipulation Primitives

| Symbol | Name | Description | Stack Effect |
|--------|------|-------------|--------------|
| `D` | Duplicate | Copies the top element | 1 → 2 |
| `w` | Swap | Swaps the top two elements | 2 → 2 |
| `x` | Delete | Removes the top element | 1 → 0 |
| `U` | Unpack | Pushes each element of an array onto the stack individually | 1 → n |
| `Ls` | Stack Length | Pushes the current number of elements on the stack | 0 → 1 |

### Clipboards

RATL provides four clipboards for temporary storage outside the stack:

- `H`, `G`, `L`, `M` — each holds one value independently of the stack
- Useful for saving intermediate results without stack gymnastics

***

## Literals

Literals are pushed onto the stack as soon as they are encountered during parsing.

### Numbers
Standard numeric notation: `1`, `10`, `3.14`, `-5`.

### Numerical Arrays (Vectors)
Enclosed in square brackets `[...]`. Elements are separated by spaces.
Example: `[1 2 3]` pushes a numeric vector of length 3.

### Character Arrays (Strings)
Enclosed in single quotes `'...'`.
Example: `'hello'` pushes a character vector of length 1.

### Cell Arrays (Lists)
Enclosed in curly braces `{...}`.
Example: `{1 'a' [1 2]}` pushes an R list containing three different types.

***

## Blocks and Evaluation Model

Blocks are first-class code fragments enclosed in `{...}`. They are **not** executed when pushed — they are pushed as values and later consumed by higher-order operators.

### Block Creation
```
{2 *}    → pushes a block onto the stack (value, not execution)
{+}      → pushes a block that adds the top two elements
```

### Block Consumption

Blocks are consumed by operators that pop them from the stack:

| Operator | Name | Behavior |
|----------|------|----------|
| `{block} array q` | Map | Applies block to each element, collects results into array |
| `{block} array e` | Filter | Keeps elements where block returns truthy |
| `{block} array y` | Reduce | Folds array left using block as binary operator |
| `N {block} z` | Repeat | Executes block N times, returns last result |
| `{block} @` | Execute | Pops and executes a block immediately |
| `{block} array zB` | Sort By | Sorts array using block as key function |
| `{block} array zG` | Group By | Groups array elements by block's return value |
| `{block} array zC` | Scan | Like reduce, but returns all intermediate results |
| `{block} array zT` | Take While | Takes elements while block returns truthy |
| `{block} array zW` | Drop While | Drops elements while block returns truthy |
| `{block} A B zZ` | Zip With | Applies block pairwise to two arrays |

### Execution Environment

When a block executes, the following rules apply:

1. **Stack access**: The block can read the stack via `$1`, `$2`, etc.
2. **Element binding**: For `q`, `e`, `zB`, `zG`, `zT`, `zW` — the current element is pushed before block execution, available as `$1`
3. **Return value**: The last value remaining on the block's internal stack after execution is the result
4. **Stack isolation**: The block's internal stack operations do not leak into the outer stack — only the return value is pushed back
5. **Nesting**: Blocks may contain other block literals; nested blocks are pushed as values, not executed

### Examples
```
# Map: double each element
# Block {2 *} receives each element as $1
[1 2 3] {2 *} q    → [2 4 6]

# Filter: keep positive numbers
# Block {0 >} receives each element; truthy keeps it
[3 -1 4 -2] {0 >} e    → [3 4]

# Reduce: sum
# Block {+} receives accumulator as $2, current element as $1
[1 2 3 4] {+} y    → 10

# Sort by: absolute value
# Block {0-} computes key for each element
[3 -1 4 -2] {0-} zB    → -1 -2 3 4

# Execute: run immediately
{3 5 +} @    → 8
```

***

## Truthiness

RATL follows R's truthiness rules. Truthiness is used by control flow (`?`), filter (`e`), while loops (double-quote), take-while (`zT`), and drop-while (`zW`).

**Falsy** values:

| Value | Reason |
|-------|--------|
| `FALSE` | Boolean false |
| `0` | Numeric zero |
| `""` | Empty string |
| `NULL` | Null value |
| `NA` | Missing value |
| `numeric(0)` | Empty numeric vector |
| `character(0)` | Empty character vector |
| `logical(0)` | Empty logical vector |

**Truthy** values: everything else, including:

- `TRUE`
- Any non-zero number (positive or negative)
- Any non-empty string
- Any non-empty vector or list
- Matrices and data frames

**Edge case — `NA`**: In R, `NA` is neither `TRUE` nor `FALSE`, but for RATL control flow it is treated as **falsy**.

***

## Control Flow

### Conditional Branching (`? ... ]`)

- **`?`** starts a conditional block, **`]`** ends it
- **If-Else**: `? condition ; then-branch ; else-branch ]`
- **Behavior**: Pops the top element. If truthy, executes until first `;` or `]`. If falsy, skips to the `;` branch (if present) or skips to `]`.
```
1 ? 'yes' ; 'no' ]        → 'yes'    # truthy
0 ? 'yes' ; 'no' ]        → 'no'     # falsy
0 ? 'yes' ]                → (empty)  # no else branch
```

### While/Repeat Loop (`" ... "`)

- **Symbol**: `"` (opening and closing)
- **Behavior**: Executes the loop body. At the closing `"`, pops the top element. If truthy, repeats from the opening `"`. If falsy, exits the loop.
```
3 " 1 - D p "    → 3 2 1    # counts down
```

### For-Each Loop (`( ... )`)

- **`(`** pops an array/list, **`)`** marks the end
- **Behavior**: Iterates through each element, pushing it onto the stack and executing the body.
```
[1 2 3] ( p )        → 1 2 3    # prints each element
[3 5 7] ( D + )       → 6 10 14  # doubles each
```

### Infinite Loop (`` ` ... ` ``)

- **Symbol**: `` ` `` (backtick, opening and closing)
- **Behavior**: Executes the body repeatedly. Use `X` (break) to exit the loop.
```
` 'quit' i X `        → 'quit'    # reads until 'quit'
```

***

## Statements and Separators

- **Space**: Acts as a token separator.
- **Newline**: Ignored by the parser but acts as a token separator.
- **Comments**: Lines starting with `#` are treated as comments and ignored.

***

## Implicit Actions

### Initial Actions
- The stack is initialized as an empty `list()`.
- A connection to `stdin` is opened for input operations.

### Final Actions
- **Implicit Print**: If the stack contains exactly one element, it is printed automatically. If it contains more than one, the entire stack is printed as a list.
- **Invisible Output**: The final evaluation result is returned invisibly in R.

***

## Meta-Programming

RATL supports meta-programming — code that generates or manipulates other code at runtime.

### Dynamic Evaluation (`ev`)

Pops a string from the stack and executes it as RATL code:

```
'3 5 +' ev        → 8
'[1 2 3] s' ev    → 6
'5 fp' ev         → 120
```

### Dispatch List (`zD`)

Pushes a list of all available symbol names onto the stack:

```
zD    → [+ - * / D w x ...]
```

### Dynamic Dispatch (`zS`)

Calls a symbol by name (string):

```
3 5 '+' zS    → 8
5 'sq' zS     → 2.236...
```

### Shell Exec (`zX`)

Runs a shell command and pushes the output:

```
'echo hello' zX    → "hello"
'ls' zX            → file listing
```

### Sort By (`zB`)

Sorts an array using a block as the key function:

```
[3 1 4 1 5] {0-} zB    → 1 1 3 4 5 (reverse sort)
[1 2 3] {2 *} zB       → 1 2 3
```

### Group By (`zG`)

Groups array elements by the block's return value:

```
[1 2 3 4 5 6] {2 %} zG    → $0: [2 4 6] $1: [1 3 5]
```

### Scan (`zC`)

Like reduce, but returns all intermediate results:

```
[1 2 3 4 5] {+} zC    → 1 3 6 10 15
[2 3 4] {*} zC        → 2 6 24
```

### Take While (`zT`)

Takes elements while the block returns truthy:

```
[1 2 3 4 5] {4 <} zT    → 1 2 3
```

### Drop While (`zW`)

Drops elements while the block returns truthy:

```
[1 2 3 4 5] {3 <} zW    → 3 4 5
```

### Zip With (`zZ`)

Applies a block pairwise to two arrays:

```
[1 2 3] [4 5 6] {+} zZ    → 5 7 9
```

***

## Error Handling

RATL propagates R errors when operations fail. Common error conditions:

| Condition | Error | Example |
|-----------|-------|---------|
| Stack underflow | `stack underflow` | `x` on empty stack |
| Type mismatch | `non-numeric argument` | `+` applied to strings |
| Divide by zero | Returns `Inf` or `NaN` | `1 0 /` |
| Index out of bounds | `subscript out of bounds` | `el` with invalid index |
| Malformed block | `unexpected }` | `{2 *` (missing closing `}`) |
| Invalid file path | `cannot open connection` | `F` with nonexistent path |

***

## Symbol Reference

**424 symbols** organized by category. Click a category to expand.

<details><summary><strong>Arithmetic & Comparison</strong> — Tier 1 (Core) — 15 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `%` | Modulo | `$2 %% $1` | 2 → 1 |
| `*` | Multiply | `$2 * $1` | 2 → 1 |
| `+` | Add | `$2 + $1` | 2 → 1 |
| `-` | Subtract | `$2 - $1` | 2 → 1 |
| `/` | Divide | `$2 / $1` | 2 → 1 |
| `<` | Less | `$2 < $1` | 2 → 1 |
| `<=` | LessEqual | `$2 <= $1` | 2 → 1 |
| `=` | Equal | `$2 == $1` | 2 → 1 |
| `>` | Greater | `$2 > $1` | 2 → 1 |
| `>=` | GreatEqual | `$2 >= $1` | 2 → 1 |
| `^` | Power | `$2 ^ $1` | 2 → 1 |
| `lX` | Xor | `xor($2, $1)` | 2 → 1 |
| `mI` | IntDiv | `$2 %/% $1` | 2 → 1 |
| `|` | Or | `$2 | $1` | 2 → 1 |
| `~` | Not | `!$1` | 1 → 1 |

</details>

<details><summary><strong>Stack & Control</strong> — Tier 1 (Core) — 11 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `D` | Duplicate | `list($1, $1)` | 1 → 2 |
| `Ls` | Stack Length | `length(stack)` | 0 → 1 |
| `U` | Unpack | `for(x in $1) stack[[length(stack)+1]] <- x` | 1 → 0 |
| `i` | Input | `scan(ratl_stdin, what=character(), n=1, quiet=TRUE)` | 0 → 1 |
| `p` | Print | `ratl_print($1)` | 1 → 0 |
| `sC` | Cat | `cat($1)` | 1 → 0 |
| `sM` | Message | `message($1)` | 1 → 0 |
| `sS` | Stop | `stop($1)` | 1 → 0 |
| `sW` | Warning | `warning($1)` | 1 → 0 |
| `w` | Swap | `list($1, $2)` | 2 → 2 |
| `x` | Delete | `NULL` | 1 → 0 |

</details>

<details><summary><strong>Constants</strong> — Tier 3 (Extensions) — 6 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `In` | Inf | `Inf` | 0 → 1 |
| `Na` | NA | `NA` | 0 → 1 |
| `Pi` | Pi | `pi` | 0 → 1 |
| `s4` | EmptyLog | `logical(0)` | 0 → 1 |
| `sQ` | EmptyChar | `character(0)` | 0 → 1 |
| `sZ` | EmptyNum | `numeric(0)` | 0 → 1 |

</details>

<details><summary><strong>Higher-Order Functions</strong> — Tier 2 (Toolkit) — 17 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `Fa` | Apply | `apply($3, $2, $1)` | 3 → 1 |
| `Ff` | Filter (Func) | `Filter($2, $1)` | 2 → 1 |
| `Fl` | Lapply | `lapply($2, $1)` | 2 → 1 |
| `Fm` | Mapply | `mapply($1, ...)` | 1 → 1 |
| `Fn` | Find (Func) | `Find($2, $1)` | 2 → 1 |
| `Fp` | Position | `Position($2, $1)` | 2 → 1 |
| `Fq` | Map (evaluator) | `NULL` | 0 → 1 |
| `Fr` | Reduce | `Reduce($2, $1)` | 2 → 1 |
| `Fs` | Sapply | `sapply($2, $1)` | 2 → 1 |
| `Ft` | Filter (evaluator) | `NULL` | 0 → 1 |
| `Fv` | Vapply | `vapply($3, $2, $1)` | 3 → 1 |
| `Fx` | Repeat (evaluator) | `NULL` | 0 → 1 |
| `e` | Filter (evaluator) | `NULL` | 0 → 1 |
| `fn` | Negate | `Negate($1)` | 1 → 1 |
| `q` | Map (evaluator) | `NULL` | 0 → 1 |
| `y` | Reduce (evaluator) | `NULL` | 0 → 1 |
| `z` | Repeat (evaluator) | `NULL` | 0 → 1 |

</details>

<details><summary><strong>Array Operations</strong> — Tier 1 (Core) — 44 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `#` | Filter | `$2[$1]` | 2 → 1 |
| `,` | Concat Vectors | `c($2, $1)` | 2 → 1 |
| `.` | Range 1 to N | `1:$1` | 1 → 1 |
| `..` | Pair | `list($2, $1)` | 2 → 1 |
| `:` | Sequence | `seq($2, $1)` | 2 → 1 |
| `O` | Sort | `sort($1)` | 1 → 1 |
| `R` | Reverse | `rev($1)` | 1 → 1 |
| `SH` | Head | `head($1)` | 1 → 1 |
| `SR` | Rank | `rank($1)` | 1 → 1 |
| `ST` | Tail | `tail($1)` | 1 → 1 |
| `XC` | CumSum | `cumsum($1)` | 1 → 1 |
| `Xk` | RLE | `{r <- rle($1); list(r$values, r$lengths)}` | 1 → 2 |
| `c1` | CumProd | `cumprod($1)` | 1 → 1 |
| `cn` | IsIn | `is.element($2, $1)` | 2 → 1 |
| `dO` | Order | `order($1)` | 1 → 1 |
| `dS` | Split | `split($2, $1)` | 2 → 1 |
| `dU` | Unsplit | `unsplit($2, $1)` | 2 → 1 |
| `el` | Extract Element [[ | `$2[[$1]]` | 2 → 1 |
| `en` | Extract Name $ | `$2[[as.character($1)]]` | 2 → 1 |
| `es` | Extract Subset [ | `$2[$1]` | 2 → 1 |
| `fE` | FirstElem | `head($1, 1)` | 1 → 1 |
| `fu` | Flatten | `unlist($1)` | 1 → 1 |
| `hd` | HeadN | `head($2, $1)` | 2 → 1 |
| `ix` | IndexOf | `match($2, $1)` | 2 → 1 |
| `l` | Length | `length($1)` | 1 → 1 |
| `la` | LastElem | `tail($1, 1)` | 1 → 1 |
| `ns` | NegSlice | `$2[length($2)+1-$1]` | 2 → 1 |
| `r1` | Range1toN | `1:$1` | 1 → 1 |
| `r2` | Rep | `rep($2, $1)` | 2 → 1 |
| `tb` | Tabulate | `tabulate($1)` | 1 → 1 |
| `tl` | TailN | `tail($2, $1)` | 2 → 1 |
| `u` | Unique | `unique($1)` | 1 → 1 |
| `un` | UniqueN | `length(unique($1))` | 1 → 1 |
| `vB` | PMin | `pmin($2, $1)` | 2 → 1 |
| `vD` | Drop | `drop($1)` | 1 → 1 |
| `vF` | Diff | `diff($1)` | 1 → 1 |
| `vG` | PMax | `pmax($2, $1)` | 2 → 1 |
| `vH` | WhichArr | `which($1, arr.ind=TRUE)` | 1 → 1 |
| `vI` | CumMin | `cummin($1)` | 1 → 1 |
| `vT` | RepMat | `matrix(rep($3, $2*$1), nrow=nrow($3)*$2)` | 3 → 1 |
| `vW` | SeqLen | `seq($3, $2, length.out=$1)` | 3 → 1 |
| `vX` | CumMax | `cummax($1)` | 1 → 1 |
| `wh` | Which | `which($1)` | 1 → 1 |
| `zp` | Zip | `unlist(Map(list, $2, $1))` | 2 → 1 |

</details>

<details><summary><strong>Matrix</strong> — Tier 2 (Toolkit) — 28 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `!` | Transpose | `t($1)` | 1 → 1 |
| `&` | Outer Product | `$2 %o% $1` | 2 → 1 |
| `BT` | Bingo Twin | `{idx<-c(1,4,3,2,5); $1[idx,idx]}` | 1 → 1 |
| `R9` | Rot90 | `apply(t($1), 2, rev)` | 1 → 1 |
| `Y!` | Create Matrix | `matrix($3, nrow=$2, ncol=$1)` | 3 → 1 |
| `Y*` | Matrix Mult | `$2 %*% $1` | 2 → 1 |
| `YM` | Matrix ByRow | `matrix($3, nrow=$2, ncol=$1, byrow=TRUE)` | 3 → 1 |
| `v2` | Solve 2 | `solve($2, $1)` | 2 → 1 |
| `vC` | Col Indices | `col($1)` | 1 → 1 |
| `vE` | Row Sums | `rowSums($1)` | 1 → 1 |
| `vL` | Solve | `solve($1)` | 1 → 1 |
| `vM` | Col Means | `colMeans($1)` | 1 → 1 |
| `vN` | Row Means | `rowMeans($1)` | 1 → 1 |
| `vQ` | QR Decomp | `qr($1)` | 1 → 1 |
| `vR` | Row Indices | `row($1)` | 1 → 1 |
| `vS` | Col Sums | `colSums($1)` | 1 → 1 |
| `vV` | SVD | `svd($1)` | 1 → 1 |
| `y!` | Diag | `diag($1)` | 1 → 1 |
| `yD` | Identity Matrix | `diag($1)` | 1 → 1 |
| `yc` | EigenVectors | `eigen($1)$vectors` | 1 → 1 |
| `yd` | Determinant | `det($1)` | 1 → 1 |
| `yf` | Full | `as.matrix($1)` | 1 → 1 |
| `yk` | Kronecker | `kronecker($2, $1)` | 2 → 1 |
| `yl` | TriLower | `{x<-$1; x[upper.tri(x)]<-0; x}` | 1 → 1 |
| `ym` | Create Matrix | `matrix($3, nrow=$2, ncol=$1)` | 3 → 1 |
| `yt` | Trace | `sum(diag($1))` | 1 → 1 |
| `yu` | TriUpper | `{x<-$1; x[lower.tri(x)]<-0; x}` | 1 → 1 |
| `yv` | EigenValues | `eigen($1)$values` | 1 → 1 |

</details>

<details><summary><strong>Statistics</strong> — Tier 3 (Extensions) — 42 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `B5` | FiveNum | `fivenum($1)` | 1 → 1 |
| `BC` | Covariance | `cov($2, $1)` | 2 → 1 |
| `BS` | Summary | `summary($1)` | 1 → 1 |
| `Ba` | MAD | `mad($1)` | 1 → 1 |
| `Bc` | Correlation | `cor($2, $1)` | 2 → 1 |
| `Bi` | IQR | `IQR($1)` | 1 → 1 |
| `Bn` | Min | `min($1)` | 1 → 1 |
| `Br` | Range | `range($1)` | 1 → 1 |
| `Bs` | Standard Deviation | `sd($1)` | 1 → 1 |
| `Bx` | Max | `max($1)` | 1 → 1 |
| `Bz` | Scale | `scale($1)` | 1 → 1 |
| `Ku` | Kurtosis | `sum(($1-mean($1))^4)/((length($1)-1)*sd($1)^4)` | 1 → 1 |
| `P` | Product | `prod($1)` | 1 → 1 |
| `Q` | Quantile | `quantile($1)` | 1 → 1 |
| `Sd` | CumSD | `ratl_cum_sd($1)` | 1 → 1 |
| `Sk` | Skewness | `sum(($1-mean($1))^3)/((length($1)-1)*sd($1)^3)` | 1 → 1 |
| `Sm` | MeanNA | `mean($1, na.rm=TRUE)` | 1 → 1 |
| `Sn` | SumNA | `sum($1, na.rm=TRUE)` | 1 → 1 |
| `Sw` | WeightedMean | `weighted.mean($2, $1)` | 2 → 1 |
| `V` | Variance | `var($1)` | 1 → 1 |
| `Xo` | Mode | `ratl_mode($1)` | 1 → 1 |
| `Xr` | RMS | `ratl_rms($1)` | 1 → 1 |
| `Xs` | Std Dev | `sd($1)` | 1 → 1 |
| `cr` | Correlation | `cor($2, $1)` | 2 → 1 |
| `cv` | Covariance | `cov($2, $1)` | 2 → 1 |
| `dT` | Table | `table($1)` | 1 → 1 |
| `dX` | XTabs | `xtabs($2, $1)` | 2 → 1 |
| `h` | Median | `median($1)` | 1 → 1 |
| `lA` | Any | `any($1)` | 1 → 1 |
| `lZ` | NNZ | `sum($1 != 0)` | 1 → 1 |
| `m` | Mean | `mean($1)` | 1 → 1 |
| `mQ` | Quantile | `quantile($1, $2)` | 2 → 1 |
| `mn` | Min2 | `min($2, $1)` | 2 → 1 |
| `mx` | Max2 | `max($2, $1)` | 2 → 1 |
| `r` | Random (n) | `runif($1)` | 1 → 1 |
| `rm` | RMS | `ratl_rms($1)` | 1 → 1 |
| `s` | Sum | `sum($1)` | 1 → 1 |
| `sd` | Standard Deviation | `sd($1)` | 1 → 1 |
| `sm` | Summary | `summary($1)` | 1 → 1 |
| `v` | Variance | `var($1)` | 1 → 1 |
| `vA` | All | `all($1)` | 1 → 1 |
| `vd` | Density | `density($1)` | 1 → 1 |

</details>

<details><summary><strong>Statistical Modeling</strong> — Tier 3 (Extensions) — 29 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `K!` | Coef | `coef($1)` | 1 → 1 |
| `KA` | AOV | `aov($1)` | 1 → 1 |
| `KB` | BIC | `BIC($1)` | 1 → 1 |
| `KE` | Residuals | `residuals($1)` | 1 → 1 |
| `KF` | Fitted | `fitted($1)` | 1 → 1 |
| `KL` | LogLik | `logLik($1)` | 1 → 1 |
| `KV` | VCov | `vcov($1)` | 1 → 1 |
| `av` | Anova | `anova($1)` | 1 → 1 |
| `dA` | Aggregate | `aggregate($3, $2, $1)` | 3 → 1 |
| `dE` | Cut | `cut($2, $1)` | 2 → 1 |
| `dY` | By | `by($3, $2, $1)` | 3 → 1 |
| `ka` | Anova | `anova($1)` | 1 → 1 |
| `ke` | Predict | `predict($2, $1)` | 2 → 1 |
| `kg` | GLM | `glm($1)` | 1 → 1 |
| `ki` | AIC | `AIC($1)` | 1 → 1 |
| `kl` | Linear Model | `lm($1)` | 1 → 1 |
| `kn` | NLS | `nls($1)` | 1 → 1 |
| `ko` | Loess | `loess($1)` | 1 → 1 |
| `lm` | Linear Model | `lm($1)` | 1 → 1 |
| `mC` | Conf Int | `confint($1)` | 1 → 1 |
| `mL` | LM Simple | `lm($2 ~ $1)` | 2 → 1 |
| `mM` | Model Matrix | `model.matrix($1)` | 1 → 1 |
| `mO` | Offset | `offset($1)` | 1 → 1 |
| `mP` | Predict | `predict($1)` | 1 → 1 |
| `mR` | Model Frame | `model.frame($1)` | 1 → 1 |
| `mT` | Terms | `terms($1)` | 1 → 1 |
| `mU` | Update | `update($2, $1)` | 2 → 1 |
| `mW` | Formula | `formula($1)` | 1 → 1 |
| `vp` | PCA | `prcomp($1)` | 1 → 1 |

</details>

<details><summary><strong>Distributions & Tests</strong> — Tier 3 (Extensions) — 86 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `DN` | Density Normal | `dnorm($3, $2, $1)` | 3 → 1 |
| `KC` | Cor Test | `cor.test($2, $1)` | 2 → 1 |
| `KK` | Kruskal-Wallis | `kruskal.test($1)` | 1 → 1 |
| `N` | Random Normal | `rnorm($1)` | 1 → 1 |
| `PN` | Prob Normal | `pnorm($3, $2, $1)` | 3 → 1 |
| `QN` | Quantile Normal | `qnorm($3, $2, $1)` | 3 → 1 |
| `RB` | Random Beta | `rbeta($3, $2, $1)` | 3 → 1 |
| `RE` | Random Exponential | `rexp($2, $1)` | 2 → 1 |
| `RN` | Random Normal | `rnorm($3, $2, $1)` | 3 → 1 |
| `RU` | Random Uniform | `runif($3, $2, $1)` | 3 → 1 |
| `dB` | Density Binomial | `dbinom($3, $2, $1)` | 3 → 1 |
| `dC` | Density Chi-Square | `dchisq($2, $1)` | 2 → 1 |
| `dG` | Density Geometric | `dgeom($2, $1)` | 2 → 1 |
| `dL` | Density Logistic | `dlogis($3, $2, $1)` | 3 → 1 |
| `dN` | Density Neg-Binomial | `dnbinom($3, $2, $1)` | 3 → 1 |
| `dP` | Density Poisson | `dpois($2, $1)` | 2 → 1 |
| `db` | Density Beta | `dbeta($3, $2, $1)` | 3 → 1 |
| `dc` | Density Cauchy | `dcauchy($3, $2, $1)` | 3 → 1 |
| `de` | Density Exponential | `dexp($2, $1)` | 2 → 1 |
| `df` | Density F | `df($3, $2, $1)` | 3 → 1 |
| `dg` | Density Gamma | `dgamma($3, $2, $1)` | 3 → 1 |
| `dh` | Density Hypergeometric | `dhyper($4, $3, $2, $1)` | 4 → 1 |
| `dl` | Density Log-Normal | `dlnorm($3, $2, $1)` | 3 → 1 |
| `dn` | Density Normal | `dnorm($1)` | 1 → 1 |
| `dt` | Density Student-t | `dt($2, $1)` | 2 → 1 |
| `du` | Density Uniform | `dunif($3, $2, $1)` | 3 → 1 |
| `kb` | Bartlett Test | `bartlett.test($1)` | 1 → 1 |
| `kc` | Chi-Square Test | `chisq.test($1)` | 1 → 1 |
| `kf` | Fisher Test | `fisher.test($1)` | 1 → 1 |
| `kk` | KS Test | `ks.test($2, $1)` | 2 → 1 |
| `kp` | Prop Test | `prop.test($2, $1)` | 2 → 1 |
| `ks` | Shapiro-Wilk | `shapiro.test($1)` | 1 → 1 |
| `kt` | T-Test | `t.test($2, $1)` | 2 → 1 |
| `kv` | F-Test Var | `var.test($2, $1)` | 2 → 1 |
| `kw` | Wilcoxon Test | `wilcox.test($2, $1)` | 2 → 1 |
| `pB` | Prob Binomial | `pbinom($3, $2, $1)` | 3 → 1 |
| `pC` | Prob Chi-Square | `pchisq($2, $1)` | 2 → 1 |
| `pG` | Prob Geometric | `pgeom($2, $1)` | 2 → 1 |
| `pL` | Prob Logistic | `plogis($3, $2, $1)` | 3 → 1 |
| `pN` | Prob Neg-Binomial | `pnbinom($3, $2, $1)` | 3 → 1 |
| `pP` | Prob Poisson | `ppois($2, $1)` | 2 → 1 |
| `pb` | Prob Beta | `pbeta($3, $2, $1)` | 3 → 1 |
| `pc` | Prob Cauchy | `pcauchy($3, $2, $1)` | 3 → 1 |
| `pe` | Prob Exponential | `pexp($2, $1)` | 2 → 1 |
| `pf` | Prob F | `pf($3, $2, $1)` | 3 → 1 |
| `pg` | Prob Gamma | `pgamma($3, $2, $1)` | 3 → 1 |
| `ph` | Prob Hypergeometric | `phyper($4, $3, $2, $1)` | 4 → 1 |
| `pl` | Prob Log-Normal | `plnorm($3, $2, $1)` | 3 → 1 |
| `pn` | Prob Normal | `pnorm($1)` | 1 → 1 |
| `pt` | Prob Student-t | `pt($2, $1)` | 2 → 1 |
| `pu` | Prob Uniform | `punif($3, $2, $1)` | 3 → 1 |
| `pw` | Prob Weibull | `pweibull($3, $2, $1)` | 3 → 1 |
| `qB` | Quantile Binomial | `qbinom($3, $2, $1)` | 3 → 1 |
| `qC` | Quantile Chi-Square | `qchisq($2, $1)` | 2 → 1 |
| `qG` | Quantile Geometric | `qgeom($2, $1)` | 2 → 1 |
| `qL` | Quantile Logistic | `qlogis($3, $2, $1)` | 3 → 1 |
| `qN` | Quantile Neg-Binomial | `qnbinom($3, $2, $1)` | 3 → 1 |
| `qP` | Quantile Poisson | `qpois($2, $1)` | 2 → 1 |
| `qb` | Quantile Beta | `qbeta($3, $2, $1)` | 3 → 1 |
| `qc` | Quantile Cauchy | `qcauchy($3, $2, $1)` | 3 → 1 |
| `qe` | Quantile Exponential | `qexp($2, $1)` | 2 → 1 |
| `qf` | Quantile F | `qf($3, $2, $1)` | 3 → 1 |
| `qg` | Quantile Gamma | `qgamma($3, $2, $1)` | 3 → 1 |
| `qh` | Quantile Hypergeometric | `qhyper($4, $3, $2, $1)` | 4 → 1 |
| `ql` | Quantile Log-Normal | `qlnorm($3, $2, $1)` | 3 → 1 |
| `qn` | Quantile Normal | `qnorm($1)` | 1 → 1 |
| `qt` | Quantile Student-t | `qt($2, $1)` | 2 → 1 |
| `qu` | Quantile Uniform | `qunif($3, $2, $1)` | 3 → 1 |
| `qw` | Quantile Weibull | `qweibull($3, $2, $1)` | 3 → 1 |
| `rB` | Random Binomial | `rbinom($3, $2, $1)` | 3 → 1 |
| `rC` | Random Chi-Square | `rchisq($2, $1)` | 2 → 1 |
| `rG` | Random Geometric | `rgeom($2, $1)` | 2 → 1 |
| `rL` | Random Logistic | `rlogis($3, $2, $1)` | 3 → 1 |
| `rN` | Random Neg-Binomial | `rnbinom($3, $2, $1)` | 3 → 1 |
| `rP` | Random Poisson | `rpois($2, $1)` | 2 → 1 |
| `rb` | Random Binomial | `rbinom($1, size=1, prob=0.5)` | 1 → 1 |
| `rc` | Random Cauchy | `rcauchy($3, $2, $1)` | 3 → 1 |
| `re` | Random Exp | `rexp($1)` | 1 → 1 |
| `rf` | Random F | `rf($3, $2, $1)` | 3 → 1 |
| `rg` | Random Gamma | `rgamma($3, $2, $1)` | 3 → 1 |
| `rh` | Random Hypergeometric | `rhyper($4, $3, $2, $1)` | 4 → 1 |
| `rl` | Random Log-Normal | `rlnorm($3, $2, $1)` | 3 → 1 |
| `rp` | Random Poisson | `rpois($1, lambda=1)` | 1 → 1 |
| `rt` | Random Student-t | `rt($2, $1)` | 2 → 1 |
| `rw` | Random Weibull | `rweibull($3, $2, $1)` | 3 → 1 |
| `tt` | T-Test | `t.test($2, $1)` | 2 → 1 |

</details>

<details><summary><strong>Math Functions</strong> — Tier 3 (Extensions) — 28 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `SP` | Sin(pi*x) | `sinpi($1)` | 1 → 1 |
| `a2` | ArcTan2 | `atan2($2, $1)` | 2 → 1 |
| `aC` | ArcCos | `acos($1)` | 1 → 1 |
| `aS` | ArcSin | `asin($1)` | 1 → 1 |
| `aT` | ArcTan | `atan($1)` | 1 → 1 |
| `ab` | Abs | `abs($1)` | 1 → 1 |
| `ac` | ArcCosh | `acosh($1)` | 1 → 1 |
| `as` | ArcSinh | `asinh($1)` | 1 → 1 |
| `at` | ArcTanh | `atanh($1)` | 1 → 1 |
| `ch` | Cosh | `cosh($1)` | 1 → 1 |
| `cl` | Ceiling | `ceiling($1)` | 1 → 1 |
| `cp` | Cos(pi*x) | `cospi($1)` | 1 → 1 |
| `e-` | Exp(x)-1 | `expm1($1)` | 1 → 1 |
| `fl` | Floor | `floor($1)` | 1 → 1 |
| `l+` | Log(1+x) | `log1p($1)` | 1 → 1 |
| `l1` | Log 10 | `log10($1)` | 1 → 1 |
| `l2` | Log 2 | `log2($1)` | 1 → 1 |
| `lg` | Log Natural | `log($1)` | 1 → 1 |
| `mc` | Cos | `cos($1)` | 1 → 1 |
| `ms` | Sin | `sin($1)` | 1 → 1 |
| `mt` | Tan | `tan($1)` | 1 → 1 |
| `ro` | Round | `round($1)` | 1 → 1 |
| `sg` | Sign | `sign($1)` | 1 → 1 |
| `sh` | Sinh | `sinh($1)` | 1 → 1 |
| `sq` | Sqrt | `sqrt($1)` | 1 → 1 |
| `th` | Tanh | `tanh($1)` | 1 → 1 |
| `tp` | Tan(pi*x) | `tanpi($1)` | 1 → 1 |
| `tr` | Trunc | `trunc($1)` | 1 → 1 |

</details>

<details><summary><strong>Combinatorics & Special</strong> — Tier 3 (Extensions) — 18 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `LG` | Log Gamma | `lgamma($1)` | 1 → 1 |
| `MC` | Choose | `choose($2, $1)` | 2 → 1 |
| `XP` | Prime Factors | `ratl_prime_factors($1)` | 1 → 1 |
| `Xn` | Choose (nCr) | `choose($2, $1)` | 2 → 1 |
| `Xq` | Is Prime | `ratl_is_prime($1)` | 1 → 1 |
| `di` | Digamma | `digamma($1)` | 1 → 1 |
| `fa` | Factors | `ratl_factors($1)` | 1 → 1 |
| `fp` | Factorial | `factorial($1)` | 1 → 1 |
| `g` | GCD | `ratl_gcd($2, $1)` | 2 → 1 |
| `lb` | Log Beta | `lbeta($2, $1)` | 2 → 1 |
| `lc` | LCM | `ratl_lcm($2, $1)` | 2 → 1 |
| `mB` | Beta | `beta($2, $1)` | 2 → 1 |
| `mG` | Gamma | `gamma($1)` | 1 → 1 |
| `mf` | Prime Factors | `ratl_prime_factors($1)` | 1 → 1 |
| `ml` | LCM | `ratl_lcm($2, $1)` | 2 → 1 |
| `mp` | Is Prime | `ratl_is_prime($1)` | 1 → 1 |
| `ps` | Psigamma | `psigamma($2, $1)` | 2 → 1 |
| `tg` | Trigamma | `trigamma($1)` | 1 → 1 |

</details>

<details><summary><strong>Complex Numbers</strong> — Tier 3 (Extensions) — 5 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `cA` | Arg | `Arg($1)` | 1 → 1 |
| `cI` | Imag Part | `Im($1)` | 1 → 1 |
| `cJ` | Conjugate | `Conj($1)` | 1 → 1 |
| `cM` | Modulus | `Mod($1)` | 1 → 1 |
| `cR` | Real Part | `Re($1)` | 1 → 1 |

</details>

<details><summary><strong>String Operations</strong> — Tier 2 (Toolkit) — 20 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `C` | Concat | `paste0($2, $1)` | 2 → 1 |
| `J` | Join List | `paste($2, collapse=$1)` | 2 → 1 |
| `S` | Split String | `strsplit($2, $1)[[1]]` | 2 → 1 |
| `S!` | Translate | `chartr($2, $1, $3)` | 3 → 1 |
| `SG` | GSub | `gsub($2, $1, $3)` | 3 → 1 |
| `Xc` | Char Translate | `chartr($1, $2, $3)` | 3 → 1 |
| `Xt` | ToLower | `tolower($1)` | 1 → 1 |
| `j` | Join | `paste($2, $1)` | 2 → 1 |
| `rv` | Reverse String | `paste(rev(strsplit(as.character($1), "")[[1]]), collapse="")` | 1 → 1 |
| `sB` | Grepl | `grepl($2, $1)` | 2 → 1 |
| `sG` | Grep | `grep($2, $1)` | 2 → 1 |
| `sL` | ToLower | `tolower($1)` | 1 → 1 |
| `sR` | Regexpr | `regexpr($2, $1)` | 2 → 1 |
| `sU` | ToUpper | `toupper($1)` | 1 → 1 |
| `sX` | Gregexpr | `gregexpr($2, $1)` | 2 → 1 |
| `sf` | Sprintf | `sprintf($2, $1)` | 2 → 1 |
| `sn` | NChar | `nchar($1)` | 1 → 1 |
| `sr` | Sub | `sub($2, $1, $3)` | 3 → 1 |
| `ss` | Substr | `substr($3, $2, $1)` | 3 → 1 |
| `st` | TrimWS | `trimws($1)` | 1 → 1 |

</details>

<details><summary><strong>Set Operations</strong> — Tier 2 (Toolkit) — 3 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `sD` | SetDiff | `setdiff($2, $1)` | 2 → 1 |
| `sI` | Intersect | `intersect($2, $1)` | 2 → 1 |
| `sN` | Union | `union($2, $1)` | 2 → 1 |

</details>

<details><summary><strong>Bitwise Operations</strong> — Tier 1 (Core) — 6 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `bA` | BitAnd | `bitwAnd($2, $1)` | 2 → 1 |
| `bL` | BitShiftL | `bitwShiftL($2, $1)` | 2 → 1 |
| `bN` | BitNot | `bitwNot($1)` | 1 → 1 |
| `bO` | BitOr | `bitwOr($2, $1)` | 2 → 1 |
| `bR` | BitShiftR | `bitwShiftR($2, $1)` | 2 → 1 |
| `bX` | BitXor | `bitwXor($2, $1)` | 2 → 1 |

</details>

<details><summary><strong>Type & Introspection</strong> — Tier 1 (Core) — 26 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `A` | To ASCII | `utf8ToInt($1)` | 1 → 1 |
| `AS` | As Type | `as($2, $1)` | 2 → 1 |
| `Xb` | To Binary | `ratl_bin($1)` | 1 → 1 |
| `Xd` | To Date | `as.Date($1)` | 1 → 1 |
| `a` | To String | `as.character($1)` | 1 → 1 |
| `c` | To Char | `intToUtf8($1)` | 1 → 1 |
| `is` | Is Type | `is($2, $1)` | 2 → 1 |
| `mF` | Factor | `factor($1)` | 1 → 1 |
| `n` | To Number | `as.numeric($1)` | 1 → 1 |
| `oA` | Attr | `attr($2, $1)` | 2 → 1 |
| `oC` | Colnames | `colnames($1)` | 1 → 1 |
| `oa` | Attributes | `attributes($1)` | 1 → 1 |
| `oc` | Class | `class($1)` | 1 → 1 |
| `od` | Dim | `dim($1)` | 1 → 1 |
| `on` | Names | `names($1)` | 1 → 1 |
| `or` | Rownames | `rownames($1)` | 1 → 1 |
| `ot` | Typeof | `typeof($1)` | 1 → 1 |
| `ou` | Unlist | `unlist($1)` | 1 → 1 |
| `zB` | Sort By | `NULL` | 2 → 1 |
| `zC` | Scan | `NULL` | 2 → 1 |
| `zD` | Dispatch List | `NULL` | 0 → 1 |
| `zG` | Group By | `NULL` | 2 → 1 |
| `zT` | Take While | `NULL` | 2 → 1 |
| `zW` | Drop While | `NULL` | 2 → 1 |
| `zZ` | Zip With | `NULL` | 3 → 1 |
| `zd` | To Date | `as.Date($1)` | 1 → 1 |

</details>

<details><summary><strong>File I/O</strong> — Tier 3 (Extensions) — 20 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `F` | Read File | `readLines($1)` | 1 → 1 |
| `FL` | List Files | `list.files($1)` | 1 → 1 |
| `FW` | Write File | `{writeLines(as.character($2), $1); NULL}` | 2 → 0 |
| `W` | Write File | `writeLines(as.character($2), $1); NULL` | 2 → 0 |
| `fA` | Dirname | `dirname($1)` | 1 → 1 |
| `fC` | Write CSV | `write.csv($2, $1)` | 2 → 0 |
| `fD` | List Dirs | `list.dirs($1)` | 1 → 1 |
| `fN` | File Create | `file.create($1)` | 1 → 1 |
| `fP` | Abs Path | `normalizePath($1)` | 1 → 1 |
| `fT` | Temp File | `tempfile()` | 0 → 1 |
| `fX` | Dir Exists | `dir.exists($1)` | 1 → 1 |
| `fb` | Basename | `basename($1)` | 1 → 1 |
| `fc` | Read CSV | `read.csv($1)` | 1 → 1 |
| `fd` | Dir Create | `dir.create($1)` | 1 → 1 |
| `fe` | File Exists | `file.exists($1)` | 1 → 1 |
| `fi` | File Info | `file.info($1)` | 1 → 1 |
| `fm` | File Remove | `file.remove($1)` | 1 → 1 |
| `fr` | Read Table | `read.table($1)` | 1 → 1 |
| `ft` | Temp Dir | `tempdir()` | 0 → 1 |
| `fw` | Write Table | `write.table($2, $1)` | 2 → 0 |

</details>

<details><summary><strong>System</strong> — Tier 3 (Extensions) — 20 symbols</summary>

| Symbol | Description | R Code | Stack Effect |
|--------|-------------|--------|--------------|
| `T` | Time Epoch | `as.numeric(Sys.time())` | 0 → 1 |
| `ZT` | Timezone | `Sys.timezone()` | 0 → 1 |
| `ge` | Getenv | `Sys.getenv($1)` | 1 → 1 |
| `pi` | GetPID | `Sys.getpid()` | 0 → 1 |
| `sE` | Date | `date()` | 0 → 1 |
| `sF` | SetPrompt | `options(prompt=$1)` | 1 → 0 |
| `sH` | GetWD | `getwd()` | 0 → 1 |
| `sJ` | SetWD | `setwd($1)` | 1 → 0 |
| `sK` | Toc | `proc.time() - $1` | 1 → 1 |
| `sP` | Sleep | `Sys.sleep($1)` | 1 → 0 |
| `sT` | Tic | `proc.time()` | 0 → 1 |
| `sV` | RVersion | `R.version.string` | 0 → 1 |
| `sY` | SetScipen | `options(scipen=$1)` | 1 → 0 |
| `se` | Setenv | `Sys.setenv(...)` | 1 → 0 |
| `um` | Umask | `Sys.umask($1)` | 1 → 1 |
| `zg` | GC | `gc()` | 0 → 1 |
| `zl` | Locale | `Sys.localeconv()` | 0 → 1 |
| `zo` | Options | `options()` | 0 → 1 |
| `zt` | SysTime | `Sys.time()` | 0 → 1 |
| `zv` | Version | `version` | 0 → 1 |

</details>

***

## Formal Grammar

```
program     ::= statement*
statement   ::= literal | symbol | block

literal     ::= number | array | string
number      ::= [ '-' ] DIGIT+ [ '.' DIGIT+ ]
array       ::= '[' statement* ']'
string      ::= Apostrophe { NOT_APOSTROPHE } Apostrophe

block       ::= '{' statement* '}'

symbol      ::= <any 1-2 character token not matching the above>
comment     ::= '#' { NOT_NEWLINE } NEWLINE

# Whitespace rules:
# - Space and newline act as token separators
# - Newlines are otherwise ignored (not significant)
```

***

## Design Rationale

### Why R?
R provides powerful statistical computing, matrix operations, and a rich standard library. This lets RATL achieve golf-friendly conciseness for mathematical and statistical tasks that would require hundreds of characters in other languages.

### Why Stack-Based?
Stack-based languages eliminate the need for variable names and assignment syntax. In code golf, every character counts — a stack model means `3 5 +` instead of `result = a + b`.

### Why These Control Flow Delimiters?
The delimiters (`?`, `]`, `(`, `)`, `"`, `` ` ``) were chosen for single-character compression. Block delimiters `{` and `}` follow convention and remain memorable.

### Why Not Purely Functional?
RATL mixes functional higher-order operations with imperative stack manipulation because R itself is multi-paradigm. This pragmatic approach maximizes expressiveness for golf.

### Implementation
RATL is implemented in R and uses R's internal data structures (vectors, lists, matrices) to handle stack operations. It translates RATL code into R code, which is then evaluated. An alternative implementation would need to replicate this stack-to-R-code translation.

